#
# wm independent hotkeys
#

# web browser
super + w
	$BROWSER

# screen shot
Print
	flameshot screen -p $HOME/Screenshots

shift + Print
	flameshot gui

# make sxhkd reload its configuration files
super + Escape
	pkill -USR1 -x sxhkd

# increase audio volume
XF86AudioRaiseVolume
	volume.sh up

# decrease audio volume
XF86AudioLowerVolume
	volume.sh down

# toggle mute
XF86AudioMute
	volume.sh mute

# close topmost notification
ctrl + Delete
	dunstctl close

# close all notifications currently displayed
ctrl + shift + Delete
	dunstctl close-all
